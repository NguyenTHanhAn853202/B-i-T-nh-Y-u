import Render from './backgroundCheckLove/background'
// render tất cả các giao diện ở đây
function RenderCheckLove() {
    return ( 
        <div style={{position:'fixed', width:'100%'}}>
            <Render />
        </div>
    );
}

export default RenderCheckLove;